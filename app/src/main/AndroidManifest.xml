<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".workmanager.WorkManagerActivity"></activity>
        <activity android:name=".ui.main.RoomDBActivity" />
        <activity android:name=".FlowRealTimeActivity" />
        <activity android:name=".FlowActivity" />
        <activity android:name=".LiveDataActivity" />
        <activity android:name=".StructuredConcurrencyActivity" />
        <activity android:name=".GlobalScopeActivity" />
        <activity android:name=".BasicsActivity" />
        <activity android:name=".CallBackActivity" />
        <activity android:name=".ScopeSecondActivity" />
        <activity android:name=".ScopeActivity" />
        <activity android:name=".ParallelActivity" />
        <activity android:name=".SequentialActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!--
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="http" />
            </intent-filter>
            -->
        </activity>
    </application>

</manifest>